// lcm(a,b) * gcd(a,b) = a*b

// co-prime number => gcd(a,b) = 1
// co-prime are those whose hcf = 1, i.e. no common factor except 1

// Modulo Arithmetics:

// a%n = [0 -> n-1]
// ex: 
// 10%5 -> 0
// 11%5 -> 1
// 12%5 -> 2
// 13%5 -> 3
// 14%5 -> 4
// 15%5 -> 0
// 16%5 -> 1

// i.e. for 5, remainder will range b/w [0-4]

// ----------------------------------

// (a+b)%m = (a%m + b%m) % m
// (a-b)%m = (a%m - b%m + m) % m
// (a*b)%m = (a%m) * (b%m)





